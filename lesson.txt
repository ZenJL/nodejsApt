$=== lecture 1 ===
Synchronous === wait, step by step
Asynchronous === multi action


=== lecture 2 ===
npm ... => run code in scripts

app.use ko url -> match all -> proceed
app.use co url -> match url -> proceed

app.use -> if qualify on url -> ordering specific url from top to bottom:
e.g: "/root/abc" -> "/root" -> "/"

app.use -> can use to "GET" / "POST"
app.get or app.post -> check correct url


=== lecture 2 ===
convert image to base64 => pass link to url for quick demo UI
write ejs like php: <% >  <% >

<%= > => output value

=== lecture 3 ===
controller: handle logic from request
view: UI
modal:

c2 sua loi path undefined o navigation
(typeof path !== 'undefined') ? (path==='/'? 'active' : "") : ""

library "multer"
1. dinh nghia storage chua file upload
2. 


/ tuyet doi,
ko "/" => tuong doi